import { sayHello, data as logdata, color } from './utils';
// Default Export
import value from './utils'; 

import loggedinUser, { user } from './login';  

loggedinUser(user);

sayHello();

console.log(logdata);

value();

