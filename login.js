const loggedinUser = user => {
    console.log(`${user} logged In`);
    console.log(user);
};

// const user = 'xyz';
// console.log(user);

// login('abc');


export { loggedinUser as default, user};